services:
  app:
    build:
      context: .
      args:
        UID: ${UID:-1000}
        GID: ${GID:-1000}
    image: book-directory-api:dev
    user: "${UID:-1000}:${GID:-1000}"
    restart: unless-stopped
    env_file: .env
    volumes:
      - .:/var/www/html:delegated
    networks:
      - book-directory-api_net
    depends_on:
      mysql: { condition: service_healthy }
      redis: { condition: service_healthy }

  worker:
    image: book-directory-api:dev
    user: "${UID:-1000}:${GID:-1000}"
    restart: unless-stopped
    command: php artisan queue:work --queue=high,default --tries=3 --timeout=90
    env_file: .env
    volumes:
      - .:/var/www/html:delegated
    networks:
      - book-directory-api_net
    depends_on:
      app: { condition: service_started }

  scheduler:
    image: book-directory-api:dev
    user: "${UID:-1000}:${GID:-1000}"
    restart: unless-stopped
    command: php artisan schedule:work
    env_file: .env
    volumes:
      - .:/var/www/html:delegated
    networks:
      - book-directory-api_net
    depends_on:
      app: { condition: service_started }

  nginx:
    image: nginx:alpine
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - .:/var/www/html:ro
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - book-directory-api_net
    depends_on:
      - app

  mysql:
    image: mysql:8.0
    restart: unless-stopped
    env_file: .env
    ports:
      - "33061:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - book-directory-api_net
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-p${DB_PASSWORD}"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - book-directory-api_net
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s

networks:
  book-directory-api_net:
    driver: bridge

volumes:
  mysql_data:
  redis_data:
